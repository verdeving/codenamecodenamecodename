#if iris_major >= 0 and iris_middle >= 0 and iris_minor >= 1
#include "Optic.hpp"

namespace Iris::Optic
{
    static auto translate(const std::string& file, const Lens::IR& ir) noexcept -> void
    {
        
    }

    auto optic(const std::string file, const Cornea::CompilationUnit& , Lens::IR&) noexcept
    -> void
    {

    }
}

#endif